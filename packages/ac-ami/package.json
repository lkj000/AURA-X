{
  "name": "@aura-x/ac-ami",
  "version": "0.1.0",
  "private": true,
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "dev": "tsc --watch",
    "test": "jest --no-coverage"
  },
  "dependencies": {
    "@aura-x/ctl": "workspace:*",
    "@aura-x/engine": "workspace:*",
    "@aura-x/replicate-client": "workspace:*",
    "zod": "^3.22.4"
  },
  "devDependencies": {
    "@aura-x/suno-exporter": "workspace:*",
    "@types/jest": "^29.0.0",
    "@types/node": "^25.6.0",
    "jest": "^29.0.0",
    "ts-jest": "^29.0.0",
    "typescript": "^5.0.0"
  },
  "jest": {
    "preset": "ts-jest",
    "testEnvironment": "node",
    "testPathIgnorePatterns": [
      "<rootDir>/dist/"
    ]
  }
}

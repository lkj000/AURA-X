[variables]
PYTHONPATH = "."

[phases.setup]
nixPkgs = ["python311Full"]

[phases.install]
cmds = [
  "python3 -m venv /opt/venv",
  "/opt/venv/bin/pip install -r requirements.txt"
]

[start]
cmd = "/opt/venv/bin/python -m uvicorn src.api:app --host 0.0.0.0 --port $PORT"

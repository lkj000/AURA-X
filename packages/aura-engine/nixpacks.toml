[variables]
PYTHONPATH = "."

[phases.setup]
nixPkgs = ["python311Full"]

[phases.install]
cmds = ["python3 -m pip install -r requirements.txt"]

[start]
cmd = "python3 -m uvicorn src.api:app --host 0.0.0.0 --port $PORT"
